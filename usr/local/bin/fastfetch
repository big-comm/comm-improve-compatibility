#!/bin/bash
if [[ -f "$HOME/.config/fastfetch/ascii-art" ]]; then
    /usr/bin/fastfetch --file-raw "$HOME/.config/fastfetch/ascii-art" "$@"
else
    /usr/bin/fastfetch --file-raw /etc/fastfetch/ascii-art "$@"
fi
